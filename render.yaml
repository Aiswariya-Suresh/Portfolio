# Render Blueprint — static frontend only, $0/month forever.
services:
  - type: web
    name: aiswariya-portfolio
    runtime: static
    rootDir: frontend
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
